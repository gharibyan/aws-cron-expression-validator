{
  "name": "aws-cron-expression-validator",
  "version": "1.0.0",
  "description": "Parse aws event bridge cron expression",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "standard",
    "lint:fix": "standard --fix"
  },
  "keywords": ["EventBridge", "aws", "cron", "expression"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "date-fns": "^2.29.3"
  },
  "devDependencies": {
    "standard": "11.0.1"
  }
}
